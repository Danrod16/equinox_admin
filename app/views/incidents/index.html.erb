<div class="wrapper-header">
  <h1> <%= t('.incidents') %> </h1>
  <%= link_to t('incidents.new_incident'), new_incident_path, class: "button-primary" %>
</div>

<div class="card-wrapper">
  <%= render 'shared/table.html.erb', { headers: Incident::INCIDENT_HEADERS, records: @incidents } %>
</div>
