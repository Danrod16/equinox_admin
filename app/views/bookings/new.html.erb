<h1>New Booking</h1>
<div class="card-wrapper">
  <%= render 'shared/form_booking.html.erb', { booking: @booking, class_list: "" } %>
</div>

<!-- Button trigger modal -->
<button type="button" class="btn btn-primary button-modal d-none" data-toggle="modal" data-target="#exampleModal">
  Launch modal
</button>

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Insert New Form</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%#= render 'shared/form_user.html.erb', { user: @user, class_list: "d-none" } %>
        <%= render 'shared/form_flat.html.erb', { flat: @flat, class_list: "d-none" } %>
        <%= render 'shared/form_tenant.html.erb', { tenant: @tenant, class_list: "d-none" } %>
      </div>
    </div>
  </div>
</div>
