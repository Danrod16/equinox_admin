<div class="wrapper-header">
  <h1> <%= t('shared.sidebar.invoices') %> </h1>
  <%= link_to t('table.new_invoice'), new_invoice_path, class: "button-primary" %>
</div>

<div class="card-wrapper">
  <%= render 'shared/table.html.erb', { headers: Invoice::INVOICE_HEADERS, records: @invoices } %>
</div>
